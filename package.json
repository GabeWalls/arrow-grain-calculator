{
  "name": "arrow-grain-calculator",
  "version": "1.0.0",
  "description": "Arrow Weight Calculator - Calculate arrow and bolt grain weights",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node bin/www",
    "install-backend": "cd backend && npm install",
    "install-frontend": "cd frontend && npm install",
    "install-all": "npm run install-backend && npm run install-frontend",
    "heroku-postbuild": "npm run install-all && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GabeWalls/arrow-grain-calculator.git"
  },
  "keywords": [
    "arrow",
    "calculator",
    "archery"
  ],
  "author": "",
  "license": "ISC"
}
