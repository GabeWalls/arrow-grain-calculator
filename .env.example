# MongoDB Connection String
# Get this from MongoDB Atlas (https://www.mongodb.com/cloud/atlas) or your MongoDB instance
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/arrow-weight-calculator?retryWrites=true&w=majority

# Node Environment
# Set to 'production' for production deployment
NODE_ENV=development

# React App API URL (for frontend)
# In production, this should be empty or set to your domain
# The frontend will use relative URLs when this is not set
REACT_APP_API_URL=
